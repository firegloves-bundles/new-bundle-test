descriptorVersion: v2

code: new-bundle-test
description: This is the bundle to test the new structure

components:
  plugins:
    - plugins/my-plugin.yaml

  assets:
    - assets/jail/jail.yaml

  widgets:
    - widgets/details-widget.yaml
    - widgets/form-widget.yaml
    - widgets/table-widget.yaml
# entando-needle-descriptor-add-widgets - Entando blueprint will add widget bundle files here

